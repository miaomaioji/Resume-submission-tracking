export function SettingsPage() {
  return (
    <section>
      <h2 className="text-base font-semibold">设置</h2>
      <p className="mt-2 text-sm" style={{ color: 'var(--text-muted)' }}>
        渠道管理、超时阈值(全局 + 按渠道)、主题与语言将在 P2/P4 实现。
      </p>
    </section>
  )
}
